app:
  title: Backstage Status Page Plugin
  baseUrl: http://localhost:3000
backend:
  cors:
    origin: http://localhost:3000
    methods: [GET, HEAD, PATCH, POST, PUT, DELETE, OPTIONS]
    credentials: true
  reading:
    allow:
      - host: localhost
